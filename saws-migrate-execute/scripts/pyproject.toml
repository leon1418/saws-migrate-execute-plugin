[project]
name = "saws-migrate-execute-scripts"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.35,<2",
    "botocore>=1.35,<2",
]

[dependency-groups]
dev = ["pytest>=8"]
